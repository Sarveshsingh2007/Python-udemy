from random import randint
dice_images = ["❶", "❷", "❸", "❹", "❺", "❻"]

# dice_num = randint(1, 6) # it will give the error due to index out of range 

dice_num = randint(0, 5)
print(dice_images[dice_num])
